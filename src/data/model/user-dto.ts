export type UserDto = {
    userId: string,
    name: string,
    email: string,
    hashedPassword: string,
    birthday: string,
    accessToken: string,
    refreshToken: string,
    googleId: string
}
